# Planning and review artifacts — local only, never committed
/docs/
CLAUDE.md

# Claude Code local session state
/.claude/

# PHP dependencies (dev tooling only; lockfile is committed for reproducible CI)
/vendor/

# JavaScript dependencies and build output
/node_modules/
/build/

# Playwright output and stored admin sessions
/artifacts/
/playwright-report/
/test-results/

# Local environment
.wp-env.override.json
*.log

# PHPUnit test-run cache, machine-local
.phpunit.result.cache

# Editor and OS noise
.DS_Store
.idea/
.vscode/
*.swp
